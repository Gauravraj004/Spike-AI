# Provider: groq (RECOMMENDED - fast, cheap), google, or ollama (slow)
PROVIDER=groq

# Groq API Key (Get free key from: https://console.groq.com/)
GROQ_API_KEY=your_groq_api_key_here

# Optional: Google Gemini (if using PROVIDER=google)
GOOGLE_API_KEY=your_google_api_key_here

# LLM Models (auto-configured based on provider)
# Groq: llama-3.1-8b-instant (text), meta-llama/llama-4-scout-17b-16e-instruct (vision)
# LLM_MODEL=llama-3.1-8b-instant
# VLM_MODEL=meta-llama/llama-4-scout-17b-16e-instruct

# Disable LLM entirely (use rule-based only - fastest!)
DISABLE_LLM=false

# Enable VLM stage 4 (vision model for final check)
ENABLE_VLM=true

# Filter to single case for testing
# ONLY_CASE=example_site
